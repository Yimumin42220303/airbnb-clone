# 배포 가이드

airbnb-clone 배포 방법과 Vercel 팀/프로젝트 구분을 안내합니다.

---

## Cron (Hobby 플랜 제한)

Vercel Hobby 플랜은 cron이 **하루 1회**만 실행 가능합니다.

- `charge-deferred`: 매일 0시 (자정)
- `beds24-price-sync`: 매일 6시 (6시간마다 → 1회/일로 조정)

Pro 플랜으로 업그레이드하면 `beds24-price-sync`를 6시간마다(`0 */6 * * *`)로 변경 가능합니다.

---

## 팀·프로젝트 구분

| 팀 | 프로젝트 | 프로덕션 URL | 용도 |
|----|----------|--------------|------|
| **minbaktokyos-projects** | airbnb-clone | https://tokyominbak.net | 운영 사이트 |
| **chris-projects** | airbnb-clone | https://airbnb-clone-six-hazel-53.vercel.app | GitHub 연동 테스트 등 |

- **tokyominbak.net** 배포 → minbaktokyos 사용
- GitHub push 시 자동 배포되는 곳은 **chris-projects** (Git 저장소 homepage)
- Deploy Hook·CLI 배포는 `.env`에서 지정한 팀/프로젝트에 적용됨

---

## 권장: CLI 직접 배포 (가장 확실)

로컬 코드를 Vercel에 바로 올립니다. GitHub·Deploy Hook 없이 동작합니다.

### 1. .env에 추가 (minbaktokyos = tokyominbak.net)

```
VERCEL_ORG_ID="team_1qdVcZtzKQGHJmkdtvE2xrHZ"
VERCEL_PROJECT_ID="prj_z477oVao1phDDrLMETtRQlkTXMYH"
```

### 2. 배포 실행

```bash
npm run deploy:cli
```

- Vercel CLI 로그인 필요: `vercel login`
- `.vercel/project.json`이 자동 생성됨 (gitignore)

---

## Git push + Deploy Hook

1. `npm run deploy` → git add, commit, push
2. push 후 Deploy Hook 호출 (설정 시)
3. Deploy Hook은 Vercel 프로젝트 Settings → Git → Deploy Hooks에서 생성

**주의**: Deploy Hook은 **해당 프로젝트가 연결된 저장소/브랜치** 기준으로 배포합니다.  
minbaktokyos용 Hook은 minbaktokyos 프로젝트에서, chris용 Hook은 chris 프로젝트에서 생성해야 합니다.

- minbaktokyos 설정: `npm run setup:deploy-hook` (기본)
- chris-projects: `VERCEL_TEAM=chris npm run setup:deploy-hook`

---

## 배포가 안 될 때

1. **Vercel 대시보드에서 팀 확인**  
   - tokyominbak.net → minbaktokyos-projects  
   - airbnb-clone-six-hazel-53 → chris-projects

2. **CLI 직접 배포로 우회**  
   - `.env`에 VERCEL_ORG_ID, VERCEL_PROJECT_ID 설정  
   - `npm run deploy:cli`

3. **GitHub 연동 확인**  
   - Vercel → 프로젝트 → Settings → Git  
   - GitHub 저장소 연결 여부 확인  
   - GitHub → 저장소 → Settings → Webhooks에서 Vercel webhook 에러 확인

4. **Deploy Hook 동작**  
   - Deploy Hook 응답은 성공인데 배포가 안 보이면  
   - 해당 프로젝트가 아닌 다른 팀 대시보드를 보고 있을 수 있음

5. **Beds24 가격이 표시되지 않을 때**  
   - Vercel → 프로젝트 → Settings → Environment Variables  
   - `BEDS24_REFRESH_TOKEN` 이 Production/Preview 환경에 설정되어 있는지 확인  
   - 미설정 시 Beds24 API 호출이 실패하고 기본 가격만 표시됨

---

## 배포 실패(Error) 시 확인

Deployments 목록에서 **Error**가 나오면 아래 순서로 확인하세요.

1. **실제 오류 메시지 확인**  
   - Vercel 대시보드 → 해당 실패한 배포 클릭 → **Building** 또는 **Logs** 탭  
   - 빌드 실패인지, "Deploying outputs" 단계에서 "internal error"인지 구분

2. **`invalid-email-address`로 트리거된 경우**  
   - Git 커밋 작성자 이메일이 Vercel 계정에 없거나 미인증일 수 있음  
   - 로컬에서 확인: `git config user.email`  
   - **해결**: Vercel 대시보드 → Account Settings → 이메일 인증 완료  
   - 또는 팀 프로젝트면, 해당 이메일을 팀에 초대하고 인증된 이메일로 커밋

3. **"We encountered an internal error" (Deploying outputs 단계)**  
   - Vercel 쪽 일시적 오류인 경우가 많음  
   - **해결**: 몇 분 뒤 **다시 배포** (같은 커밋으로 "Redeploy" 버튼 또는 `npm run deploy:cli`)  
   - 계속 실패하면 Vercel Status 페이지 확인 후, 필요 시 지원 문의

4. **CLI로 우회 배포**  
   - Git push / Deploy Hook이 계속 실패하면 로컬에서 직접 올리기  
   - `.env`에 배포할 팀의 `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` 설정  
   - 팀/프로젝트 ID: Vercel → 프로젝트 → Settings → General 하단 "Project ID"  
   - `npm run deploy:cli` 실행 (빌드는 로컬이 아니라 Vercel에서 실행됨)

---

## 명령 요약

| 명령 | 설명 |
|------|------|
| `npm run deploy` | git push + (선택) Deploy Hook 트리거 |
| `npm run deploy:cli` | 로컬 → Vercel 직접 배포 (권장) |
| `npm run deploy:vercel` | Deploy Hook 또는 API로만 트리거 |
| `npm run setup:deploy-hook` | Deploy Hook 설정 페이지 열기 |
| `npm run setup:vercel-link` | .vercel/project.json 생성 (deploy:cli용) |
