# 계정 통합 가이드 (zxcv04027@gmail.com)

Vercel, GitHub, Neon을 모두 **zxcv04027@gmail.com** 하나로 통일하는 방법입니다.

---

## 목표 구성

| 서비스 | 이메일 |
|--------|--------|
| Vercel | zxcv04027@gmail.com |
| GitHub | zxcv04027@gmail.com |
| Neon | zxcv04027@gmail.com |

---

## 1단계: GitHub (zxcv04027@gmail.com)

### A) 이미 계정이 있는 경우
1. https://github.com 로그아웃
2. **zxcv04027@gmail.com**으로 로그인
3. 사용자명 확인 (예: `zxcv04027` 또는 다른 이름)

### B) 계정이 없는 경우
1. https://github.com/signup
2. **zxcv04027@gmail.com**으로 가입
3. 사용자명 설정 (예: `zxcv04027`)

### C) airbnb-clone 저장소 옮기기

**옵션 1: 새 저장소 생성 후 코드 push (권장)**

1. GitHub(zxcv04027) 로그인 상태에서
2. https://github.com/new 접속
3. Repository name: `airbnb-clone`
4. Public 선택 → **Create repository**
5. Cursor 터미널에서 실행:

```bash
cd c:\Users\zxcv0\Desktop\airbnb-clone
git remote set-url origin https://github.com/[사용자명]/airbnb-clone.git
git push -u origin main
```

※ `[사용자명]`을 실제 GitHub 사용자명으로 바꾸세요.

**옵션 2: 기존 Yimumin42220303 저장소 Transfer**

1. Yimumin42220303 계정으로 https://github.com/Yimumin42220303/airbnb-clone 접속
2. Settings → Danger Zone → Transfer ownership
3. **zxcv04027** GitHub 사용자명 입력 후 이전

---

## 2단계: Vercel (zxcv04027@gmail.com)

### A) 새 Vercel 계정으로 시작

1. https://vercel.com 로그아웃
2. **zxcv04027@gmail.com**으로 가입/로그인
3. Continue with GitHub → **zxcv04027** GitHub 계정 연결

### B) 기존 프로젝트 옮기기

**방법 1: 새 프로젝트로 Import**

1. Vercel(zxcv04027) 로그인
2. Add New → Project
3. GitHub에서 **airbnb-clone** (zxcv04027 소유) 선택
4. Import
5. 환경 변수 복사 (기존 Vercel에서 Settings → Environment Variables 확인 후 동일하게 입력)
6. 도메인 tokyominbak.net 연결 (Settings → Domains)

**방법 2: 팀에 프로젝트 이전**

- 기존 minbaktokyo 팀에 있던 프로젝트를 zxcv04027 계정으로 옮기려면, 새 프로젝트 Import 후 기존 프로젝트 삭제

---

## 3단계: Neon (zxcv04027@gmail.com)

### A) Neon 계정 확인/생성

1. https://neon.tech 로그아웃
2. **zxcv04027@gmail.com**으로 가입/로그인

### B) 데이터베이스 마이그레이션

**기존 DB 데이터 유지가 필요한 경우:**

1. 기존 Neon 프로젝트에서 Connection string 복사
2. 새 Neon(zxcv04027) 계정에서 프로젝트 생성
3. `pg_dump` / `pg_restore` 또는 Neon 내보내기로 데이터 이전
4. 새 Connection string을 Vercel 환경 변수 `DATABASE_URL`에 설정

**새로 시작해도 되는 경우:**

1. Neon(zxcv04027)에서 새 프로젝트 생성
2. Connection string 복사
3. 로컬 `.env`의 `DATABASE_URL` 업데이트
4. `npx prisma db push` 또는 `npx prisma migrate deploy` 실행
5. `npm run db:seed` (시드 필요 시)
6. Vercel 환경 변수 `DATABASE_URL` 업데이트

---

## 4단계: 전체 연결 확인

| 연결 | 확인 방법 |
|------|-----------|
| Cursor → GitHub | `git push` 성공 |
| GitHub → Vercel | push 시 자동 배포 |
| Vercel → Neon | 사이트에서 DB 연동 정상 |

---

## 요약 체크리스트

- [ ] GitHub: zxcv04027@gmail.com 로그인, airbnb-clone 저장소 있음
- [ ] Vercel: zxcv04027@gmail.com 로그인, airbnb-clone 프로젝트 Import
- [ ] Neon: zxcv04027@gmail.com 로그인, DATABASE_URL 설정
- [ ] git remote가 새 GitHub 저장소를 가리킴
- [ ] Vercel 환경 변수 (DATABASE_URL, NEXTAUTH_*, RESEND_* 등) 설정 완료
